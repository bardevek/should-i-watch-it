import styled from "styled-components";
import {ListGroup, Row} from "react-bootstrap";

export const ListContainer = styled(Row)`
  margin: 10% 0;
`;

export const ListItemContainer = styled(ListGroup)`
  width: 80%;
`;